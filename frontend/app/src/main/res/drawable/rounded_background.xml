<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Couleur de fond -->
    <solid android:color="@color/backgroundColor" />

    <!-- Coins arrondis -->
    <corners android:radius="12dp" />

    <!-- Bordure existante (modifiable) -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black" /> <!-- Bordure noire -->

    <!-- Autre bordure ou modifications (optionnel) -->
    <!-- Par exemple une bordure secondaire de couleur différente -->
    <stroke
        android:width="1dp"
        android:color="@color/tertiaryColor" />
</shape>
