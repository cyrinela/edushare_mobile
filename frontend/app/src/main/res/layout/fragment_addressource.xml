<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Champ pour le nom de la ressource -->
    <EditText
        android:id="@+id/nameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nom de la ressource"
        android:inputType="text" />

    <!-- Champ pour la description de la ressource -->
    <EditText
        android:id="@+id/descriptionEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Description de la ressource"
        android:inputType="text" />

    <!-- Spinner pour choisir la catégorie -->
    <Spinner
        android:id="@+id/categorySpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Bouton pour sélectionner un fichier -->
    <Button
        android:id="@+id/selectFileButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Choisir un fichier" />

    <!-- Champ pour afficher le nom du fichier sélectionné -->
    <EditText
        android:id="@+id/fileEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nom du fichier"
        android:inputType="text"
        android:enabled="false" />

    <!-- Bouton pour ajouter la ressource -->
    <Button
        android:id="@+id/addButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ajouter la ressource" />

</LinearLayout>
